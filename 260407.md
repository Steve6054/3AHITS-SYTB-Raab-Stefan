# Arbeitsbericht

- Name: Stefan Raab
- Datum: 07.04.2026
- Thema: Argumente
- Fach: SYTB
- Klasse: 3AHITS

## Theorie
### Script-Argumente

Hat man zum Beispiel folgenden Aufruf eines Scripts:

```bash
./myscript.sh abc xyz ...
```

Man kann im Script jetzt auch auf die einzelnen Argumente als auch den Pfad zum Script zugreifen:
- `$0`: Pfad (`./myscript.sh`)
- `$1`: Argument 1 (`abc`)
- `$2`: Argument 2 (`xyz`)
- `$@`: Liste aller Argumente

### for-Schleife

In einem Script kann auch eine `for-Schleife` verwendet werden.

Bsp:
```bash
for arg in "$@"
do
    echo "$arg"
done
```

In der Variable `$arg` ist das Argument des aktuellen Schleifendurchgangs gespeichert. Dieses Beispiel würde also alle übergebenen Argumente des Aufrufs ausgeben.

---

## Übungen
### Übung (Directory Struktur)
Schreibe ein Shell-Script das Unterverzeichnisse und Dateien anlegt, es wird ein Argument an das Script übergeben.

Aufruf: `./build_dirs.sh abcd`

Dies führt zu folgender Directorystruktur:

```bash
./
└── abcd/
    ├── abcd_01/
    │   ├── abcd.01.1.txt
    │   ├── abcd.01.2.txt
    │   └── abcd.01.3.txt
    └── abcd_02/
        ├── abcd.02.1.txt
        ├── abcd.02.2.txt
        └── abcd.02.3.txt
```

Schreibe weiters ein Shell-Script `clean_dir.sh` das diese Verzeichnisstruktur wieder löscht.

Hinweis: Das Kommando `tree` kann verwendet werden um eine Directory Struktur in obiger Form darzustellen.

---

`build_dirs.sh`:

```bash
#!/bin/env bash

mkdir $1

cd $1
mkdir ${1}_01
mkdir ${1}_02

cd ${1}_01
touch ${1}.01.1.txt
touch ${1}.01.2.txt
touch ${1}.01.3.txt
cd ..

cd ${1}_02
touch ${1}.02.1.txt
touch ${1}.02.2.txt
touch ${1}.02.3.txt
```

Die curly-Braces werden benötigt, weil das Script die Variablen sonst falsch interpretiert.

Für `clean_dir` braucht es nur einen einzigen Befehl:

```bash
#!/bin/env bash

rm -r $1
```

Jetzt führe ich die Scripts aus und sehe mir die Ordnerstruktur danach an:
```bash
┌──(kali㉿kali)-[~/260407]
└─$ chmod +x build_dirs.sh   


┌──(kali㉿kali)-[~/260407]
└─$ chmod +x clean_dir.sh 


┌──(kali㉿kali)-[~/260407]
└─$ ./build_dirs.sh abcd
                                                                                                                                                                  
┌──(kali㉿kali)-[~/260407]
└─$ tree
.
├── abcd
│   ├── abcd_01
│   │   ├── abcd.01.1.txt
│   │   ├── abcd.01.2.txt
│   │   └── abcd.01.3.txt
│   └── abcd_02
│       ├── abcd.02.1.txt
│       ├── abcd.02.2.txt
│       └── abcd.02.3.txt
├── build_dirs.sh
└── clean_dir.sh

4 directories, 8 files
                                                                                                                                                                  
┌──(kali㉿kali)-[~/260407]
└─$ ./clean_dir.sh abcd 
                                                                                                                                                                  
┌──(kali㉿kali)-[~/260407]
└─$ tree
.
├── build_dirs.sh
└── clean_dir.sh

1 directory, 2 files
```

---

### Übung (Skript Generator)

Schreibe ein Skript das

   - eine Skriptdatei erzeugt (Name wird als Argument übergeben),
   - die She-Bang Zeile einfügt,
   - einen echo Befehl einfügt und
   - das execution Flag für das Skript setzt.

Anwendung:

```bash
$ ./makescript.sh mytest
```

erzeugt die Datei `mytest.sh` :

```bash
#!/bin/env sh

echo "mytest Skript"

# write yor script here
```

Das erzeugte Skript kann sofort ausgeführt werden:

```bash
$ ./mytest.sh
```

---

`makescript.sh`:
```bash
#!/bin/env bash

touch $1.sh

echo "#!/bin/env bash" >> $1.sh
echo "echo "$1 Skript"" >> $1.sh
echo "# Write your script here" >> $1.sh

chmod +x $1.sh
```

Jetzt erstelle ich ein Test-Skript, sehe mir den Inhalt an und führe es aus:
```bash
┌──(kali㉿kali)-[~/260407]
└─$ chmod +x makescript.sh 
                                                                                                                                                                  
┌──(kali㉿kali)-[~/260407]
└─$ ./makescript.sh test


┌──(kali㉿kali)-[~/260407]
└─$ cat test.sh           
#!/bin/env bash
echo test Skript
# Write your script here
                                                                                                                                                                  
┌──(kali㉿kali)-[~/260407]
└─$ ./test.sh           
test Skript
```

---

### Übung (Headline Cat)

Verwende `$@` zur Lösung dieser Aufgabenstellung.

Schreibe ein Skript das eine Art cat zur Verfügung stellt. Als Argumente werden eine beliebige Anzahl von Textdateien übergeben. Das Ergebnis (der Inhalt aller dieser Dateien) wird in die Datei result.txt (fixer Dateiname) geschrieben (ist die Datei vorhanden soll deren Inhalt überschrieben werden). Jedem Datei-Inhalt soll eine Überschrift vorangestellt werden.

Beispiel – der Aufruf
```bash
$ ./headline_cat.sh file1.txt file2.txt file3.txt
```
ergibt die Datei result.txt mit folgendem Inhalt:
```bash
== file1.txt ==========================================
Das ist der Inhalt
der ersten Textdatei

== file2.txt ==========================================
Das ist der Inhalt#!/bin/env bash
der zweiten Textdatei

== file3.txt ==========================================
Das ist der Inhalt
der dritten Textdatei
```

---

`headline_cat.sh`:
```bash
#!/bin/env bash

> result.txt

for arg in "$@"
do
    echo "==${arg}==============" >> result.txt
    cat $arg >> result.txt
    echo "" >> result.txt
done
```

Zum Testen habe ich drei Textdateien erstellt (siehe Aufgabenstellung).

Ausführen und Überprüfen:
```bash
┌──(kali㉿kali)-[~/260407]
└─$ ./headline_cat.sh file1.txt file2.txt file3.txt
                                                                                                                                                                  
┌──(kali㉿kali)-[~/260407]
└─$ cat result.txt                                 
==file1.txt==============
Das ist der Inhalt
der ersten Textdatei
==file2.txt==============
Das ist der Inhalt
der zweiten Textdatei
==file3.txt==============
Das ist der Inhalt
der dritten Textdatei
```

---

### Übung (RANDOM)

Schreibe ein shellscript das eine beliebige Menge von Dateinamen als Parameter akzeptiert. Von jeder dieser Dateien soll eine Kopie im gleichen Verzeichnis angelegt werden. Die Kopie unterscheidet sich vom Original durch eine angefügte Zufallszahl, Beispiel:

```bash
test.txt --> test.txt.38573
```

Aufrufbeispiele:

```bash
$ ./randcp.sh test1.txt test2.txt
$ ./randcp.sh xyz1.md test3.txt abcd.dat
$ ./randcp.sh *.md
```

   - Hinweis: `$RANDOM` liefert bei jeder Verwendung eine zufällige Zahl.
       - Achtung: `#!/bin/bash` in der she-bang Zeile verwenden. sh unterstützt (in REPL) keine Zufallszahlen.

---

`randcp.sh`:
```bash
#!/bin/bash

for arg in $@
do
    cp $arg ${arg}.$RANDOM
done
```

Testen:
```bash
┌──(kali㉿kali)-[~/260407]
└─$ ./randcp.sh file1.txt file2.txt file3.txt


┌──(kali㉿kali)-[~/260407]
└─$ ls
build_dirs.sh  file1.txt        file1.txt.21456  file2.txt        file3.txt        headline_cat.sh  randcp.sh   test.sh
clean_dir.sh   file1.txt.21007  file1.txt.29474  file2.txt.15029  file3.txt.16762  makescript.sh    result.txt
```

Hinweis: Ich habe bereits vorher zum Testen `./randcp.sh file1.txt` ausgeführt, weshalb `file1.txt` öfter vorkommt.
